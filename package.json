{
  "name": "ymlconfigr",
  "version": "1.0.2",
  "description": "A package that utilizes config.yml file for environment config that you can use (e.g. keeping keys and other environment variables)",
  "main": "config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node config.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "js-yaml": "^3.14.0"
  }
}
